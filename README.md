# Language-Translator

Transformer model from scratch to translate German to English using Pytorch. Model is trained on multi30k dataset and includes methods for token embedding, positional encoding, masking, and others. To train the model on a diferent language change the TRAIN_FILE_PATH variable to the dataset path. To test the model change the VAL_FILE_PATH variable to the test dataset path.

